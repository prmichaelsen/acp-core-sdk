## Summary

Brief description of what this PR changes and why.

## Type of change

- [ ] Bug fix (incorrect pattern, broken template, wrong documentation)
- [ ] New pattern (add a new `.md` to `agent/patterns/`)
- [ ] New template file (add a new `.ts` to `agent/files/`)
- [ ] Documentation improvement
- [ ] Infrastructure change (CI, scripts, workflows)

## Checklist

- [ ] Ran `@acp.package-validate` — all checks pass
- [ ] Added entry to `CHANGELOG.md` under the appropriate version
- [ ] Updated `package.yaml` if any new files were added to `agent/files/` or `agent/patterns/`
- [ ] All internal Markdown links in `docs/` are valid
- [ ] If adding a new pattern: follows the standard pattern structure (Context, Problem, Solution, Anti-Patterns, Related Patterns)
- [ ] If adding a new template file: added to `package.yaml` templates section with description and target

## Related issues

Closes #<!-- issue number -->
